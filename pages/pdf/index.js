import { Container } from "@mui/material";
import { Box } from "@mui/system";

const Pdf = ({}) => {
  return (
    <Container>
      <Box>
        <h1>PDF</h1>
      </Box>
    </Container>
  );
};

export default Pdf;
